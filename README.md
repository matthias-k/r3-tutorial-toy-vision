# toy-vision

Tiny shared vision helpers for the r3 tutorial.
`toy_vision.extract_patches(images, ...)` samples random grayscale patches across images.
Used as a git dependency from r3 jobs (checked out to `toy_vision/`).
